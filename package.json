{
  "name": "gnome-volume-mixer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "ts-for-gir generate",
    "build": "rm -rf dist && mkdir dist && tsc && cp metadata.json dist && cp build/src/* dist && cd dist && zip -p volume-mixer * "
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "ts-for-gir": "^1.0.1",
    "typescript": "^4.0.2"
  }
}
