{
  "name": "gnome-volume-mixer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "ts-for-gir generate",
    "build": "rm -rf dist && mkdir dist && tsc && cp metadata.json dist && rollup -c ./rollup.config.js && cd dist && zip -p volume-mixer * "
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^15.0.0",
    "@rollup/plugin-typescript": "^5.0.2",
    "rollup": "^2.26.6",
    "ts-for-gir": "^1.0.1",
    "typescript": "^4.0.2"
  }
}
